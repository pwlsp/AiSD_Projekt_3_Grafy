#include <list>

#include "../include/struct_graph.h"
#include "../include/struct_edgeList.h"

bool find_edge_list(graph *L, int vertices, int from, int to);

bool find_edge_table(edgeList *eList, int vertices, int from, int to);